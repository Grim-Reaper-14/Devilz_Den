# Settings Menu

Owns configs, theme selection, input, and debug presentation. Theme-specific rendering belongs in `Frontend/Menu/Themes`, while Settings exposes user-facing selectors and persistence.
